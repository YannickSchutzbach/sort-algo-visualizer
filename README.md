# 📊 Sorting Algorithm Visualizer

[![React](https://img.shields.io/badge/React-20232A?style=for-the-badge&logo=react&logoColor=61DAFB)](https://reactjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-007ACC?style=for-the-badge&logo=typescript&logoColor=white)](https://www.typescriptlang.org/)
[![Vite](https://img.shields.io/badge/Vite-646CFF?style=for-the-badge&logo=vite&logoColor=white)](https://vitejs.dev/)

Ein interaktiver **Visualisierer für Sortieralgorithmen**, der die Funktionsweise verschiedener Algorithmen anschaulich darstellt und beim Lernen hilft.

## 🚀 Übersicht

Der **Sorting Algorithm Visualizer** ist ein Web-Projekt, entwickelt mit **React**, **TypeScript** und **Vite**, das verschiedene Sortieralgorithmen Schritt für Schritt visualisiert. Ziel ist es, ein besseres Verständnis für deren Ablauf und Unterschiede zu bekommen.

## 🛠 Features

- 📈 Live-Visualisierung von Sortieralgorithmen  
- 🔢 Anpassbare Array-Größe  
- ⏱ Steuerung der Animationsgeschwindigkeit  
- 🎨 Modernes, responsives UI  
- ⚛️ Entwickelt mit React + TypeScript  

## 📦 Installation

1. Repository klonen:
```bash
git clone https://github.com/YannickSchutzbach/sort-algo-visualizer.git
```

2. In das Projektverzeichnis wechseln:

```bash
cd sort-algo-visualizer
```

3. Abhängigkeiten installieren:

  ```bash
  npm install
  ```

4. Entwicklungsserver starten:

  ```bash
  npm run dev
  ```

5. Projekt im Browser öffnen (standardmäßig):

  ```
  http://localhost:5173
  ```

## 🧠 Anwendung

1. Wähle einen Sortieralgorithmus aus  
2. Generiere ein zufälliges Array  
3. Starte die Visualisierung  
4. Beobachte, wie der Algorithmus das Array sortiert  
5. Füge verschiedene Algorithmen hinzu und vergleiche ihre Performance (optional)

## 🧠 Unterstützte Algorithmen

| Algorithmus | Durchschnitt | Worst Case | Platzkomplexität |
| :--- | :--- | :--- | :--- |
| **Bubble Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ |
| **Quick Sort** | $O(n \log n)$ | $O(n^2)$ | $O(\log n)$ |
| **Merge Sort** | $O(n \log n)$ | $O(n \log n)$ | $O(n)$ |
| **Insertion Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ |
| **Selection Sort** | $O(n^2)$ | $O(n^2)$ | $O(1)$ |
| **Heapsort** | $O(n \log n)$ | $O(n \log n)$ | $O(1)$ |

## 🧰 Tech Stack

- ⚛️ React (UI)
- 🧪 TypeScript (Typensicherheit)
- ⚡ Vite (Build Tool)
- 🎨 Tailwind CSS (Styling)

